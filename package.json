{
  "name": "ai-cv-checker",
  "version": "1.0.0",
  "description": "AI CV Checker - Get instant ATS scores and AI-powered suggestions",
  "private": true,
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "dev": "cd backend && npm run dev",
    "start": "cd frontend && npx http-server -p 3000",
    "build": "npm install --prefix backend && npm install --prefix frontend",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend"
  },
  "keywords": ["CV", "Resume", "ATS", "AI", "Analysis"],
  "author": "Mohamed Lakhal",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.6.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  }
}

